import { Socket } from "socket.io";

export const SocketListeners =  (socket: Socket) => {
    // const auth = new Auth(socket, io)
    // const room = new Room(socket, io)
    // /* Auth */
    // socket.on('login', auth.login.bind(auth))
    // socket.on('logout', auth.logout.bind(auth))

    // /* Room */
    // socket.on('create_room', room.createRoom.bind(room))
    // /* Socket Disconnect */
    // socket.on("disconnect", (reason) => {
    //     Auth.forceCloseLogout(socket); // "ping timeout"
    // });
}